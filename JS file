searchBox.addEventListener("input", () => {

    const value = searchBox.value.trim();

    if (value) {
        getMovies(SEARCH + encodeURIComponent(value));
    } else {
        getMovies(TRENDING);
    }

});
